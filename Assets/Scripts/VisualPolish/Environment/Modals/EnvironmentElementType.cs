using UnityEngine;

namespace YourNamespace
{
    /// <summary>
    /// Identifies different types of environment elements in the desired order.
    /// </summary>
    public enum EnvironmentElementType
    {
        Base,
        Ground,
        Foliage,
        Entities,
        Rocks,
        Trees,
        GameplayTiles,
        GameplayEdges,
        GameplayObstacles
    }
}