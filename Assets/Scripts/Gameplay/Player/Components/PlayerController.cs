using ScoredProductions.StreamLinked.IRC;

public class PlayerController : PlayerListenerBase
{
    public override void HandleCommand(string commandKey, object args, string sender, TwitchMessage msg)
    {
        
    }
}
